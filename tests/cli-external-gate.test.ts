import { execFile } from "node:child_process"; import { promisify } from "node:util"; import { describe, expect, it } from "vitest"; const exec=promisify(execFile); describe("CLI external policy",()=>{it("资料问答在无检索证据时不会调用模型",async()=>{const result=await exec("npx",["tsx","src/cli.ts","资料问答 RAG"],{cwd:process.cwd()});expect(result.stdout).toContain("insufficient_evidence")})});
