# 三项 Agent P0 开发与验收

2026-09-07，用户授权完成 [专项核验](evidence/p0-verification-20260907.md) 中的三个目标。

## 固定完成条件

1. CLI 与桌面实际使用同一传输无关的 Agent 会话服务，统一运行状态、队列、纠正、审批、恢复及事件；课程操作仍由学习领域服务负责。保留旧入口和数据读取兼容。
2. 经过完整协议验证的模型轮次、待执行工具及已返回结果持久保存；审批回复接回原工具调用；纠正和显式续接保留 taskId；在模型/工具边界恢复，不自动重放结果不确定且不具备安全重试能力的操作。Provider 私有思考内容不落盘。
3. 测试失败后修改产物、使用相同参数重测能够完成；不变状态的重复循环仍受限。计划未完成时进行有界继续，无法推进则明确 blocked，不将模型声明作为实际完成。
4. Pi Codex 与 DeepSeek 的工具协议均有恢复回归；新旧会话及备份不引入越权、跨主题/会话续接或私有 Provider 状态泄露。
5. 原专项脚本的 8 项全部满足；补充共享入口、检查点、拒绝/重复审批、连续多工具、取消/崩溃及 Provider 协议测试；完整 verify 与桌面 UI 通过。真实 Provider 仅用合成内容经既有受控接口验证。

## 实施顺序

- [x] A：循环进展与完成检查；先补失败回归，再实现并验证。
- [x] B：持久执行检查点、审批原链续接、取消/恢复与 Provider 协议。
- [x] C：抽取共享 Agent 会话服务并接入 CLI/桌面，纠正输入保留任务身份，兼容现有交互与学习流程。
- [x] D：专项/完整/UI 验收、真实 Provider 受控合成核验、文档及证据更新。

保留原有 README、学习效果证据与本轮检查脚本。此次不推送、不发布安装包，不调整历史验收结论。每项仅在新标准实际通过后勾选。

首轮验收：84 文件 / 383 测试、完整 verify、四组 Electron UI、原专项 8/8、双真实 Provider 合成恢复均通过。详见 [开发 Evidence](evidence/p0-development-20260907.md)。用户随后要求多次深入复核，新增反例持续补齐边界遗漏；当前验收为 86 文件 / 419 测试，结论以[最新恢复边界核查](evidence/p0-recovery-audit-20260907.md)为准，以上冻结目标不变。本轮重跑完整、专项和 UI 验收，真实 Provider 沿用上一轮合成记录。
