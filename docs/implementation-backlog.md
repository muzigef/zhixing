# 实际开发 Backlog

> 当前状态以[对齐计划](acceptance-next-plan.md)和[最新验收](evidence/acceptance-next.md)为准。0.9 安装与平台交付见[上一轮记录](evidence/completion-0.9.md)。已完成图片、MCP/语义检索等不重复立项；Pi 已恢复可用性验证，独立子 Agent 继续暂缓。

> 历史更新：2026-09-08。历史 P0–P13 与本轮 P1/P2 分开追溯；本轮范围与证据见 [增量计划](agent-p1-p2-plan.md)。已完成能力不重复立项。

## 已完成基线

- P0：学习状态机、Reviewer、资料库/citation、记忆、运行审计、Provider 路由与 E01–E31 本地自动化验收。
- P1：完整 Topic Plan、课程/Skill 内容、带 citation 的 grounded answer、计划调整/复习与 session snapshot。
- P2：本地 OCR 和低置信度页、本地向量兼容存储与混合检索、无 Docker 的 macOS 受限执行、loopback Web/SSE 同步契约。
- P3–P8：个性化课程与自然交互、运行账本、DeepSeek 工具续写、持久连续对话及终端输出改进。
- P9：Pi Codex 偏好继承与受控文本适配；P10：独立桌面对话、双 Provider 切换和 macOS arm64 本地安装包。

详见 [任务台账](../TASKS.md) 和各阶段 Evidence。真实 Provider smoke 仍为可选项；已配置 Provider 时默认可用，设置 `ZHIXING_ALLOW_LIVE_PROVIDER=0` 可禁止调用。

## 新需求入队标准

新项目项必须包含：用户可见目标、主题与隐私边界、失败/边界测试、验收命令和 Evidence 路径。不得以“基础实现”代替已验证闭环。

## P11 已实现

- CLI/桌面共享 LearningApplication，课程/进度/资料/来源与实际产物验收。
- 完整 YAML/Plan schema 贯通、无损长段落分块、精确 chunk 引用、导入取消与重试/并发去重。
- mock 禁外发误拦截、数字主题同步路由与 CI 桌面依赖修复。
- 持久目标/约束、较早对话整理、排队/立即调整/撤回/停止/重启后手动恢复。
- 真实产物哈希、用户报告与本地 JS 测试区分、固定评测用例、分模型耗时诊断。
- Electron/SQLite ABI 准备、macOS/Windows 构建与实际包 UI 流水线、校验和和 draft release、主动版本查询。

具体已运行项以 [升级 Evidence](evidence/agent-upgrade.md) 为准。

## 当前验收边界（0.9）

| 项目 | 当前结果 | 继续验证的条件 |
| --- | --- | --- |
| Pi Codex | 原模型真实请求、工具续写、项目和已安装应用 11 项检查通过 | 未定位旧网络故障根因；短探针不证明长期稳定 |
| 双 Provider 内容质量 | 当前 Pi 20/24；DeepSeek 13/23 可评分结果全通过，另 1 次无进展失败 | 资料泛化、比较边界等仍未全过；需要未见题和独立复核 |
| macOS ARM64 / Intel x64 | 远端构建、开发和实际包五组 UI 均通过；本机 ARM64 0.9 已安装 | 实际结果和具体提交见 0.9 Evidence |
| Windows | 原生 6 项、实际 NSIS 安装与 6 个运行文件哈希、安装后五组 UI 均通过 | 证据来自真实 Windows runner；不代表任意用户 PC 均兼容 |
| MCP / 语义检索 / 通知 | 官方文件 MCP、真实 embeddinggemma、签名实包原生通知通过 | 扩大服务/语料不是本轮完成条件 |
| 真实教学研究 | 导出、盲评包、同条件协议与合成流程通过 | 真实参与者、独立评阅与实际 72 小时结果尚未获得 |
| 本机系统加密授权 | 旧本机快照和三个平台远端原生合成往返通过；重签名后的本机授权等待 | 需用户在系统界面授权；自动化不能操作 SecurityAgent |
| 正式签名、公证 | macOS 预览包完整 ad-hoc 签名；没有 Developer ID/公证 | 需要实际证书与公证配置；不冒充正式发行 |
| GitHub | 源码已推送，verify 和三种平台发行验证实际通过 | 以最终提交和 job 结果为准；没有发布正式 Release |

用户仍明确延期 **独立子 Agent**。Pi 已在本轮恢复验证并通过当前可用性检查。内容质量和外部研究/系统授权/正式签名仍未全部验收，不使用“全部完成”的表述。

## 后续产品范围

- 逐句事实核实与真实模型内容质量基准；精确 token/成本控制。
- 其他 Provider 工具续写、旧 CLI 命令完全由注册表分派、任意写操作的幂等恢复。
- 定时备份、主题删除、跨设备同步和冲突处理。
- Pi 开发工具守卫的词法边界、符号链接和参数路径强化需独立威胁模型；CLI Pi 保持空工具列表；桌面 Pi SDK 仅传递应用工具定义，不具备原生执行器。
- Linux 受限执行属于后续平台扩展；Windows 当前实现与验收见上表，无可用沙箱时不执行。

不把上述范围扩展成通用 Shell、多 Agent 或插件市场；新增范围仍需用户任务。

## 0.4 收口后的历史范围

结构化上下文、双 Provider 应用工具、持久任务、后台整理、交互卡/分支、检索改进、独立检查、技能和全量备份已落实，见 [本轮计划](agent-next-plan.md)。后续不重复立项；优先依据真实质量回归改善重复、资料边界与提示遵循。0.5 已实现上下文预算、按需工具、质量评测与复核、学习观察、有界只读并行、受控 MCP 和独立实践项目，见 [0.5 指南](agent-0.5.md)。0.5 当时已有双 Provider 合成请求证据，外部集成与平台仍待验收；0.9 补齐的结果见上表，不沿用历史待办状态。独立人工评分、真实学习效果与正式签名条件仍未获得。通用用户仓库编辑、Shell 和多 Agent 保持条件性扩展。
