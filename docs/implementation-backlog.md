# 实际开发 Backlog

> 核对日期：2026-09-05，代码基线 `6b87f51`。P0–P8 的阶段验收已完成，P9 有认证环境待办，P10 已完成 macOS arm64 本地预览验收。

## 已完成基线

- P0：学习状态机、Reviewer、资料库/citation、记忆、运行审计、Provider 路由与 E01–E31 本地自动化验收。
- P1：完整 Topic Plan、课程/Skill 内容、带 citation 的 grounded answer、计划调整/复习与 session snapshot。
- P2：本地 OCR 和低置信度页、本地向量兼容存储与混合检索、无 Docker 的 macOS 受限执行、loopback Web/SSE 同步契约。
- P3–P8：个性化课程与自然交互、运行账本、DeepSeek 工具续写、持久连续对话及终端输出改进。
- P9：Pi Codex 偏好继承与受控文本适配；P10：独立桌面对话、双 Provider 切换和 macOS arm64 本地安装包。

详见 [任务台账](../TASKS.md) 和各阶段 Evidence。真实 Provider smoke 仍为可选项；已配置 Provider 时默认可用，设置 `ZHIXING_ALLOW_LIVE_PROVIDER=0` 可禁止调用。

## 新需求入队标准

新项目项必须包含：用户可见目标、主题与隐私边界、失败/边界测试、验收命令和 Evidence 路径。不得以“基础实现”代替已验证闭环。

## P11 已实现

- CLI/桌面共享 LearningApplication，课程/进度/资料/来源与实际产物验收。
- 完整 YAML/Plan schema 贯通、无损长段落分块、精确 chunk 引用、导入取消与重试/并发去重。
- mock 禁外发误拦截、数字主题同步路由与 CI 桌面依赖修复。
- 持久目标/约束、较早对话整理、排队/立即调整/撤回/停止/重启后手动恢复。
- 真实产物哈希、用户报告与本地 JS 测试区分、固定评测用例、分模型耗时诊断。
- Electron/SQLite ABI 准备、macOS/Windows 构建与实际包 UI 流水线、校验和和 draft release、主动版本查询。

具体已运行项以 [升级 Evidence](evidence/agent-upgrade.md) 为准。

## 外部验收待办

| 项目 | 当前边界 | 验收方式 |
| --- | --- | --- |
| Pi Codex 登录与真实质量 | 沿用 Pi 配置和认证，本轮没有真实调用 | Pi 登录后运行合成问题；按固定质量集记录回答与依据 |
| DeepSeek 当前速度/质量 | 历史短请求不是当前性能基线 | 使用相同合成任务收集多轮样本；查看分模型 P50/P95 |
| Windows / Intel Mac | 流水线已具备，本机未实机验收 | 对应架构构建、安装、设置、密钥存储、任务和重启测试 |
| 正式签名、公证 | 当前为未签名预览 | 提供证书后生成并验收签名安装器 |
| GitHub Actions / 正式发布 | 已配置，远端运行结果与正式发布尚待验收 | 检查远端验证结果，手动运行发布工作流，检查 draft release，再发布 |

## 后续产品范围

- 逐句事实核实与真实模型内容质量基准；精确 token/成本控制。
- 其他 Provider 工具续写、旧 CLI 命令完全由注册表分派、任意写操作的幂等恢复。
- 学习数据整体备份、定时备份、主题删除、跨设备同步和冲突处理。
- Pi 开发工具守卫的词法边界、符号链接和参数路径强化需独立威胁模型；CLI Pi 保持空工具列表；桌面 Pi SDK 仅传递应用工具定义，不具备原生执行器。
- Windows/Linux 的受限代码执行后端；当前无沙箱时不执行。

不把上述范围扩展成通用 Shell、多 Agent 或插件市场；新增范围仍需用户任务。

## 0.4 收口后的范围

结构化上下文、双 Provider 应用工具、持久任务、后台整理、交互卡/分支、检索改进、独立检查、技能和全量备份已落实，见 [本轮计划](agent-next-plan.md)。后续不重复立项；优先依据真实质量回归改善重复、资料边界与提示遵循。真实 Pi 连接、本机语义效果、Windows/Intel 实机、签名公证和远端 CI 仍需外部验收。MCP 需具体服务需求后再实现连接，通用编码/worktree/多 Agent 保持条件性扩展。
