# Agent 架构与教学增量执行计划

> 历史计划或版本记录：下文保留当时的设计、范围及验证结果，不是当前功能清单或新的开发指令。现行实现、已完成的后续改动与待验收项见 [当前状态](current-status.md) 和 [文档导航](README.md)。

基线：`a1ba9ac`；授权：2026-09-08 用户要求执行本轮分析中的所有任务。保留已完成 P0/P1/P2，不重置历史验收。采用模块化单体，继续支持 Pi Codex 与 DeepSeek。

每个切片先写失败或边界测试，再实现、定向验证和完整 `npm run verify`；涉及桌面行为运行 UI。完成状态仅依据本轮实际结果。过程证据见 [执行记录](evidence/agent-architecture-next.md)。

| 编号 | 范围 | 验收要求 | 状态 |
| --- | --- | --- | --- |
| C01 | 四项已复现缺陷 | 公共 registry 干净安装；MCP 写后重读与真正重复区分；参数诊断完整；自定义调用统计真实 | 已通过 C01 验证 |
| C02 | 统一工具契约与结果 | 从权威定义派生模型 schema，输入/输出/权限/资源语义集中；大结果可续读且不丢状态；双 Provider 契约测试 | 已通过 C02 验证 |
| C03 | 恢复、计划修订与预算 | 未知外部写入明确核对/结束入口，不自动重放；用户改目标保留计划修订；累计用量和暂停原因可查询 | 已通过 C03 验证 |
| C04 | 教学决策 | 知识点关联真实证据，按帮助方式/误解选择教学动作；来源可追溯、撤回生效、尊重直接答案请求 | 已通过 C04 验证 |
| C05 | 证据支持检查 | 来源版本/片段可定位；不支持的数值与过强结论可诊断；验证与推断区分，保持格式检查独立 | 已通过 C05 验证 |
| C06 | 模型能力与上下文 | 显式工具/模态/预算能力；按配置预算与实际 usage 诊断；结构化目标/未完成事项、历史可定位 | 已通过 C06 验证 |
| C07 | 按需 MCP 与检索 | 无关问答零外部进程启动；缓存绑定配置/schema；检索降级可见，不破坏取消/审批/恢复 | 已通过 C07 验证 |
| C08 | 细粒度授权 | 资料、项目、外部工具权限解耦；范围内复用授权；撤回/配置更换/备份恢复失效 | 已通过 C08 验证 |
| C09 | 实践项目 | 修改前快照、预览恢复、精确差异、多文件原子校验；评估并接入可用受限 Python Runner，平台能力明确 | 已通过 C09 验证 |
| C10 | 存储、事件与交互 | 元数据索引/分页、增量事件和按需渲染；历史规模测量；恢复/排队/流末段无回退 | 已通过 C10 验证 |
| C11 | Skill | 版本/适用条件/评测关联，更新和旧版本回退状态可见，不授予执行权限 | 已通过 C11 验证 |
| C12 | 评估与交付 | 组合故障与双 Provider 契约；同知识点变式题；完整产品效果试验；构建/数据/评测版本绑定；当前文档与安装包验证 | 开发及本机验证完成；外部效果验收见下方 |

图片输入先完成能力契约与明确不支持的处理；真实图片教学接入取决于 Provider 公共接口的实际能力，不将文本转换冒充多模态。保持已有运行配额，预算机制不自动扩大额度。

外部验收：真实学习者与三天复习、独立人工评价、指定 MCP 服务、Windows/Intel 实机、签名公证依赖相应人员/设备/服务。开发测试使用合成数据；不将其替代外部验收，不读取凭据、不迁移真实用户数据、不自动正式发布。
