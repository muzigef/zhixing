# Agent 下一轮优化执行计划

> 历史计划或版本记录：下文保留当时的设计、范围及验证结果，不是当前功能清单或新的开发指令。现行实现、已完成的后续改动与待验收项见 [当前状态](current-status.md) 和 [文档导航](README.md)。

2026-09-07 当前优先级已转向[教学效果验证](learning-outcomes.md)：源码新增学前/学后/延迟检查、同模型两种教学方式、帮助声明、去重统计及备份恢复。下表保留前轮交付范围；真实学习者数据、题卷难度校准和独立解释复核是接下来的效果验收，不以功能测试代替。

> 2026-09-05，依据用户“开始依次完成所有的优化项”授权。延续现有架构，顺序实施；不将外部环境验收标记为完成。

| 顺序 | 切片 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| 1 | 真实质量基线 | 12 个合成任务、双 Provider、两次独立运行，保留答案/耗时/具名复核，独立人工评分单列；无法连接如实记录 | 基线与复测已执行；助手复核完成，人工独立评分与 Pi 完整质量集待验（9 月 7 日连通验收已通过） |
| 2 | 结构化上下文 | system、历史角色、用户请求、工具观察分离；上下文有界，材料不升级为系统指令 | 已通过 verify/UI |
| 3 | 双 Provider 受控执行 | Pi/DeepSeek 使用同一应用工具；产物生成、实验执行有权限边界和真实结果 | 已通过 verify/UI；Pi 文本/读取工具续写已验；写工具真实调用待验 |
| 4 | 持久任务恢复 | 步骤、结果、完成标准持久化；重启恢复不重复写入；失败可重试 | 已通过 verify/UI |
| 5 | 速度与推理 | 独立推理档位、首字/上下文/模型/整理耗时和 usage；整理移出首字关键路径 | 已通过 verify/UI；长回答真实复测通过 |
| 6 | 自然交互 | 问题/授权/产物/最终回答类型化；可回答卡片；编辑重发、分支和对比 | 已通过 verify/三套 UI |
| 7 | 检索与引用 | 同义词/中文查询扩展、重排和邻块；可选真实语义嵌入；区分候选依据与实际引用 | 已通过 verify/三套 UI；本机嵌入模型待验 |
| 8 | 学习成效 | 独立课程检查与反例、错因记录和复习调度；完整性、测试与掌握分开 | 已通过 verify/三套 UI |
| 9 | 扩展与桌面交付 | 桌面 Skill、按需 MCP 边界、全量备份和版本迁移；构建/实包验证；外部签名/Windows 单列 | 已通过 verify/三套实包 UI/DMG；外部平台和签名单列 |

每个行为切片先写失败或边界测试，再实现，定向测试通过后运行 `npm run verify`，涉及桌面交互另运行 UI 验证。完成记录见 `docs/evidence/agent-next.md`。

Pi 继续使用已有配置及认证，由 Pi 自己读取凭据。应用工具不开放任意 shell 或读取真实凭据。真实评测仅使用临时合成工作区，不变更用户资料。通用编程工作区、Git worktree 和多 Agent 是前次分析的条件性扩展，不混入当前学习产品迭代。

Windows/Intel Mac 实机与 Apple 签名、公证依赖对应设备或开发者账户；先完成代码、CI 和本机可验证工作。MCP 在有具体连接需求前提供明确边界，不引入未经配置的外部服务。

2026-09-07：用户在 Pi 登录后完成 [真实连通验收](evidence/pi-availability-20260907.md)，原有失败样本保留，不改写为成功。

2026-09-07 延迟专项：SSE 默认传输、逐轮诊断/可见阶段、结构化进度及减少重复查询已完成；347 个测试、开发/实包 UI、真实模型复测、最终实包七项真实任务及 DMG 校验通过，见 [0.4.1 修复记录](evidence/pi-latency-fix-20260907.md)。常驻 worker 未采用，后续按准备/建连占比评估收益与生命周期复杂度。
