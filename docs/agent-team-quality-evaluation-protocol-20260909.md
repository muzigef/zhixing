# 协作优化 V3：评测预注册

本文件在首次 V3 真实请求前写入。题目、模型选择、预算、比较方法不随新留出结果调整。历史 V2 报告保留，不覆盖、不重算成新成绩。

## 冻结条件

- 开发回归：旧 H02/H04，四组，各一次，共 8 根任务，用于暴露格式、执行和核查问题，不能作为新留出收益。
- 新留出：`src/team-quality-cases.ts` 的 Q01–Q08，四组，各一次，共 32 根任务。覆盖混杂因素、多资料冲突、含偏置与正则的梯度、资源依赖计划、带互斥依赖的优化、微任务顺序、循环终止性、条件概率和重放状态。
- 四组：普通单 Pi、单 Pi 连续四次额外自检、同模型团队、Pi 主 Agent 加 DeepSeek/Kimi 的异模型团队。主模型及两个 API 均使用产品配置，不静默换模型。
- 每组共享 16384 输出 token 上限（未知用量按本次预留值占用）、最多 12 请求；单次普通请求最多 4096，分工 1024，审查 2048。五轮自检对照与团队常见五次调用接近，但实际用量不同，不能声称严格等算力。
- 首轮回归全部使用 balanced，已观察到 DeepSeek 4096 token 截断、Kimi 150 秒超时。后续回归及首次新留出使用冻结的自动成员策略：主 Agent 和 Pi 成员 balanced；DeepSeek/Kimi 成员 quick（分别提交 thinking disabled、reasoning_effort low）。这是产品配置的实验变更，不宣称相同原生推理投入；显式设置仍优先，深入模式仍保留深入推理。成员默认 180 秒；服务商适配器自己的上限仍有效。外部根期限 240 秒，普通运行时仍有每轮 180 秒内部期限。
- 团队首轮独立、有 schema 的核查；主模型审查；必要时至多一次定向复核；最后综合。所有阶段均记入请求与用量。
- 按题号轮换四组执行顺序。仅合成问题，无历史、学习资料、项目或外部工具权限。模型不会收到 expected 和评分标准字段。
- 通过用户已提供的本机代理 127.0.0.1:15236 继承 HTTP(S) 环境，不更改系统代理或登录配置。

## 评分与质量复核

原 `scoreTeamAnswer` 不变：严格检查用户要求的 JSON、确定字段与数值容差。分别统计字段正确、执行完整、完整且正确；全失败保留分母。解释存在性不替代解释质量。

逐条复核最终解释，按源码中预先列出的三条解释条件，标为通过、部分通过、不通过或无可评解释，并记录具体理由。这是开发助手的非盲复核，非独立人类评审。报告样本数、调用、用量缺失、首字/总耗时和所有失败类别。八题一次运行只支持样本内判断，不能证明统计显著或教学效果；单 Pi 满分时明确说明上限效应。

保存新运行 JSON、代码/题目/构建身份与最终对比文档。失败类型按脱敏枚举保存，不输出凭据、请求头或私有异常字符串。若回归阶段发现新缺陷，先保留该回归运行，修复并验证，然后才运行新的留出题；首次留出后不得针对它调参并冒称独立复测。

## 命令

```bash
NODE_USE_ENV_PROXY=1 HTTPS_PROXY=http://127.0.0.1:15236 HTTP_PROXY=http://127.0.0.1:15236 NO_PROXY=localhost,127.0.0.1,::1 \
ZHIXING_DESKTOP_EXECUTABLE="$PWD/desktop/release/mac-arm64/知行.app/Contents/MacOS/知行" \
node desktop/scripts/evaluate-team.mjs --live --suite=regression
```

新留出将最后参数替换为 `--suite=quality`。测试包 0.11.0 使用受控产品 Provider 和系统安全存储；评测会话独立存入临时目录，原日常会话不作为评测输入。

## 留出前构建冻结

当前测试包：0.11.0 macOS arm64；`app.asar` SHA-256 为 `5aeb4715b3204937c76dcc9812108a40e159c968d8f64c258397571eb5c3c240`。新题源码 SHA-256 为 `3ab2966ce1d1c4b6694cc7058131bf9a54b3e2a5ae9e8e45a20b90a89517f6dd`。工程测试、源码清单和限制见[构建回执](evidence/team-quality-build-20260910.json)。仅文档和评测记录在后续补齐，运行期间不替换测试包、不改题目或评分规则。

### 首次新题运行前的签名修复与重新冻结

2026-09-10：上述临时签名包未启动新题运行。修复本地持续签名及签名校验的用途/大小写兼容后，实际包经过 718 项工程测试、七组实包 UI 及原生加密检查。首次新题使用固定签名包，其 `app.asar` SHA-256 为 `cfdf67c19462815adb117e7cd07908ff510769ac5dd3aa187da897f1d3d21199`。问题源码哈希仍为上面的 `3ab2966…f6dd`，与包内构建清单完全匹配。题目、评分、模型配置和预算没有调整。

新的完整来源记录见[固定签名构建回执](evidence/team-quality-stable-build-20260910.json)。旧回归与旧包哈希继续保留；新题运行期间不修改此包或源码评分规则。Kimi 已通过受控产品连接检查，解除此前准备阶段阻塞。
