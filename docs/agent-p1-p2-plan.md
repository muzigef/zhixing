# Agent P1 / P2 增量开发与验收

2026-09-07，依据用户“逐个任务分析并实现，一定要完成验证”的授权。基线 `b2874db`，419 项测试和三项架构 P0 已验收。下面是本轮冻结的增量范围，区别于历史 MVP 的 P1/P2。

| 顺序 | 任务 | 已有基础与本轮增量 | 完成条件 | 状态 |
| --- | --- | --- | --- | --- |
| A1 | 上下文管理 | 已有字符截取/摘要；增加输入 token 估算预算、输出预留、完整工具轮次裁剪、纠正保留和上下文诊断 | 超长中英混合会话受预算约束；保留当前请求/目标/约束/纠正；不拆工具配对、不改持久原文、不提升资料权限；恢复回归通过 | 已通过：428 测试、完整 verify、四组 UI |
| A2 | 模型调用效率 | 已有 SSE、推理档位/计时；增加按需工具发现、自动推理选择与显式覆盖，验证减少无关 schema/往返 | 模型可动态发现受权限约束的能力；已明确的档位不被覆盖；复杂任务质量门、取消/恢复不退化；连接复用以实测决定，不硬加常驻进程 | 已验证：schema 测量、真实调用/档位对照、verify/UI；不宣称全任务更快 |
| A3 | 自然交互与回答质量 | 已有提示/结构化输出；增加可测的续写重复控制、格式与引用质量诊断及对应修复 | 流式中断续写不重复已显示长前缀；代码/公式不被误裁；引用和格式异常可观察；真实合成新题复核 | 程序闭环和 verify/UI 已通过；真实复核记录残余语义问题，不能保证模型全题正确 |
| A4 | 真实任务评测 | 已有固定案例与开发助手复核；增加隔离的留出题、可导入的独立评分、失败分类及可比统计 | 不把未评分/未完成算通过；明确分母、Provider/模型条件、P50/P95及评分来源；报告可从原记录复算 | 已验证：56 次真实合成请求、评分导入与复算、447 测试；独立人工与真实学习者参与待验 |
| A5 | 学习状态与反馈 | 已有作答/错因/复习；增加跨会话、可追溯的学习观察和人工解释复核闭环 | 只从真实用户作答记录学习状态；帮助与独立作答分开；撤回/纠正、主题隔离、重启/备份及教学上下文接入经过测试 | 已通过：450 测试、完整 verify、四组 UI；人工判断仍为署名自报，不等于掌握证明 |
| B1 | 并行与渲染 | 工具串行、全文 Markdown 重排；增加显式安全只读工具的有界并行与流式渲染节流 | 写入/交互/依赖工具仍有序；检查点可恢复且结果顺序稳定；停止不丢末段；真实 Electron 断言与性能对照 | 已通过：457 测试、verify、四组 UI、合成查询对照与真实 IPC 密集输出 |
| B2 | MCP 外部工具 | 现有扩展边界；实现可配置受控连接，实际场景等待用户选择 | 协议握手/能力/调用、输入校验、超时/取消、错误脱敏及权限隔离；可经真实 Agent 入口使用，本地协议夹具验收；未指定服务不连接 | 已通过：467 测试、verify、四组 UI、本地 stdio 子进程与 Agent 审批恢复；真实服务待指定 |
| B3 | 项目级实践 | 现有单份实现/测试产物；增加限定实践目录的文件操作、差异预览与 Git 隔离工作流程 | 路径/符号链接/秘密文件防护；用户目录不被静默覆盖；审批、实际测试、失败恢复和清晰产物展示贯通 | 已通过：480 测试、verify、五组 UI；Pi/DeepSeek 真实合成修改、测试、Git 完整执行 |
| B4 | 交付 | 已有 macOS 包/Windows 构建配置；构建包含本轮改动的安装包并核查 CI/平台能力 | 当前 Mac 开发与实包 UI、DMG 校验、校验和与文档；Windows/Intel/签名仅按真实运行证据验收，缺少设备/证书保持待验 | 已完成本机交付：0.5.0 arm64 DMG/ZIP、实际包五组 UI、双生产审计 0 漏洞、校验和；外部平台/签名待验 |

各项先复现缺口，最小实现后运行定向测试、完整 `npm run verify`；桌面行为运行 `npm --prefix desktop run test:ui`。真实 Provider 仅使用临时合成内容和已配置接口。代码验证、真实模型回答质量、独立人工评分及真实学习者效果分别记录。

本轮用户授权开发这些增量，但未要求自动 Git 提交、推送或正式发布。不得读取凭据、连接未指定服务或删除真实用户资料。真实学习效果、平台实机与签名无法用本地 mock 代替。
