# 统一沙箱验收：2026-09-17

目标：统一执行策略与平台后端，审计全部原生执行入口，用实际进程验证文件、网络、生命周期和资源边界。设计与完整入口清单见[执行沙箱](../execution-sandbox.md)。

## 本地结果

- macOS arm64，Node 24.8.0；根目录和 desktop 两套 `npm ci --no-audit --no-fund` 成功。
- 新增测试先复现旧实现的缺口：资源超限仅超时/截断、没有交互式共享入口；旧实现失败记录保留在本次执行日志。
- 实际文件/网络/子进程及内存、CPU、磁盘、输出、墙钟、取消探针已通过；只使用临时合成文件和 loopback 监听器，未读取真实凭据。
- 首轮完整 verify 的 lint、两端类型检查、156 文件 / 826 测试、integration 9、eval 6、mock smoke 通过；末尾 diff 检查发现旧 Codex 文件尾部多余空行。已修复，将依据最终重跑更新结果；这些子集不重复计数。
- 桌面七组 UI 通过，包含实际 Node/Python 测试、Git 实践、会话恢复、权限交互、API/团队隔离夹具。未调用真实模型、未申请钥匙串授权、未替换已安装 App。

## 跨平台状态

新增原生 CI 矩阵：macOS ARM、macOS Intel、Windows、Linux。当前准备在独立验证分支执行；在实际 job 完成前，Windows/Linux 不能记为通过。模拟平台选择/策略测试不算原生系统验收。

Windows restricted MCP 的交互式外部读授权未实现，契约明确拒绝。macOS/Linux 内存与各平台目录配额为监控终止，不宣称瞬时硬上限；要求硬内存限制的平台不满足时拒绝。trusted MCP 和固定宿主适配器不是 OS 沙箱；审计文档逐项说明信任边界。

## 可复现命令

```bash
npm run test:sandbox -- --reporter=default --reporter=json --outputFile=.build/sandbox-local.json
CI=1 npm run verify
ZHIXING_ALLOW_LIVE_PROVIDER=0 npm --prefix desktop run test:ui
```

跨平台完整探针与 JSON 结果由 `.github/workflows/sandbox-boundaries.yml` 生成并上传。最终运行链接、提交和结果将在该工作流完成后写入本文件。
