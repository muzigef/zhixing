# 全项目文档与面试资料核验

日期：2026-09-11。基线为已推送 `60570bc`；本轮范围是文档、开发说明与面试资料，不修改应用实现、凭据、安装状态或原始评测 JSON。

## 完成内容与核查方法

原有 138 份受版本控制的 Markdown 全部纳入清单：39 份现行指南和模块说明与源码/契约/测试核对；84 份历史计划和 Evidence 检查适用范围与引用，保留原始结果；15 份内附 Skill、运行指令、Topic Plan 和测试夹具核对用途，不把测试样例改成产品文档。新增 10 份 Markdown，包括 `agent.md`、当前状态、文档导航、逐文件清单及面试资料。最终共 148 份，逐文件核查方式见[清单](../documentation-inventory.md)，状态、哈希与核验依据见[结构化记录](documentation-review-20260911.json)。

核查发现：使用指南与架构说明仍残留桌面没有学习工具、仅两家 Provider、旧会话格式、未支持图片/多 Agent、CI没有安装桌面依赖、只有macOS隔离等说法；已按当前代码修正。旧安装状态改为有日期的历史证据。会话格式按字段写入 v8–v13，并非所有新会话统一v13。

招聘调研采用中英文搜索与企业官方来源，形成 7 家机构 11 个岗位样本；补充字节/华为/小米方向信息，页面受限或只读到标题的条目不编造完整要求。面试材料覆盖 10 个领域、60 道题和 6 个系统设计案例，另附岗位匹配、14 天准备安排、项目口述与简历示例。每题包括参考答案、分析、取舍、代码/测试依据和追问；系统设计明确当前实现与假设扩展。8 个实际读取的外部技术来源及招聘访问边界见[来源说明](../interview/sources.md)。

## 发现但本轮不修改的产品问题

1. CLI `execute()` 对8,000字符以上输入提前拒绝，桌面与共享发送契约上限20,000；入口限制仍有差异。
2. [CI 34576546164](https://github.com/muzigef/zhixing/actions/runs/34576546164)：依赖双审计与verify成功；团队UI在 `smoke-team.mjs:53` 发送按钮disabled处超时。原因待复现，不把日志症状当作已确定根因。

它们已进入[当前状态](../current-status.md)与[Backlog](../implementation-backlog.md)。本轮没有改应用代码或重试远端运行，不报告已经修复。

## 验证记录

两套依赖重新安装均退出0：`npm ci --registry=https://registry.npmjs.org`、`npm ci --prefix desktop --registry=https://registry.npmjs.org`；日志位于 `desktop/.local/documentation-root-install.log` 与 `documentation-desktop-install.log`。没有修改锁文件。

| 本轮检查 | 结果与边界 |
| --- | --- |
| `CI=1 npm run verify` | 退出码 0；153 个测试文件、814 项测试通过；含脚本语法、锁文件来源、lint、两端类型检查、integration、eval、mock smoke、敏感扫描和 diff 检查 |
| integration / eval 独立重跑 | 9 项 / 6 项通过；两组已包含于全量测试，不相加为 829 个独立测试 |
| Markdown AST 本地链接与锚点 | 148 份文档全部通过；首次发现的历史 Pi worker 路径和配置锚点已修正，生成中的引用在文件完成后重新核对 |
| 题库结构 | Q01–Q60 连续且无重复，五类答题字段齐全；10 个领域、6 个系统设计案例 |
| 独立复核 | 18 份关键文档，308 个路径/命令/符号等结构断言通过，并人工核对默认值、持久化、预算、权限、恢复和能力边界；两处摘要触发与项目工具授权措辞已修正并复核，无遗留文档发现 |
| 修改范围 | 原有应用实现、两套锁文件和历史原始评测 JSON 未修改；历史结果保留；核验完成时文档尚未提交，后续提交状态以 Git 历史为准 |

全量运行日志为 `desktop/.local/documentation-verify.log`，哈希及逐文件复核结果保存在上述结构化记录中。文档检查不能替代功能实测；308 个结构断言不是 308 个语义正确性证明，也不属于产品测试数量。

历史 814 测试/七组候选包 UI/三家真实连接见[上一轮验收](provider-architecture-20260911.md)。本轮重新运行的是上述本地 verify；没有重跑桌面 UI、真实账户或完整质量实验，也没有打包安装。最新远端团队 UI 的失败仍保留，不由本轮本地通过覆盖。
