# 全量文档同步（2026-09-05）

## 依据与范围

按代码提交 `6b87f51` 核对文档。根包仍为 `0.1.0`，桌面包为 `0.2.0`；本次没有修改应用源码、依赖、CI 或构建产物。

检查范围包括根目录说明、`docs/` 使用/架构/配置/开发/测试/计划/历史 Evidence、桌面 README、运行指令、内置课程与 Skill、测试夹具说明，以及原先未跟踪的 `docs/agent-code-walkthrough.md`。沿用现有文档路径和有用内容；历史验收数字、输出与失败结果保留，并补充适用时间和后续入口。

共核对 62 份 Markdown：更新 46 份已跟踪文件和 1 份原有未跟踪代码讲解，新增 2 份文档；其余 13 份课程、Skill、运行指令或夹具内容核对后保持不变。核对阶段未执行提交或推送；随后按用户要求，将代码讲解与其余文档统一纳入提交，发布状态以 Git 历史为准。

## 主要同步

- 明确桌面对话与 CLI 学习工作流的功能边界，补齐安装、模型切换、历史/草稿、导出、快捷键和独立数据目录。
- 区分两套 Provider 默认值、CLI 角色路由与桌面偏好、Pi 全局/项目配置和运行目录、Keychain 与桌面系统加密配置。
- 区分读取到模型偏好与真实认证通过、本地安装包与公开分发、macOS arm64 验收与 Windows 配置；没有将单次 DeepSeek 耗时当成性能保证。
- 修正首次学习 RAG 的前置条件、技能草案命令名称、开发安装遗漏桌面依赖，以及完整质量门不包含 UI/安装包验证的说明。
- 按源码修正 SQLite v3、计划解析、长段落分块、引用校验、导入取消、备份范围等过度概括；RAG 课程失败案例补齐本地 OCR 已实现的分支。
- 新增 [Evidence 索引](README.md)，将旧排期和验收快照与当前指南连接。

## 本次执行结果

| 检查 | 结果 |
| --- | --- |
| 标准 `npm run verify` | 退出码 0；58 个测试文件、281 个测试通过，另跑 integration 9、eval 6、隔离 mock smoke；lint、根目录/桌面类型检查、敏感扫描及 diff 空白检查通过 |
| 额外全局禁外发探针 | `ZHIXING_ALLOW_LIVE_PROVIDER=0 ZHIXING_DESKTOP_LIVE_CHECK=0 npm run verify` 退出码 1；280/281 通过，E40“学习建议”在 Provider 之前被材料授权门拒绝。已写入测试/配置/排障文档及 Backlog，没有改代码绕过失败 |
| 文档核验 | 三组独立核对用户/开发文档、数据/行为契约、CLI/配置/历史记录；发现的文档事实问题修正后复核通过 |
| 链接与格式 | 62 份文档中的 245 个本地链接/锚点检查通过；表格分隔符检查、疑似凭据模式扫描和最终 `git diff --check` 通过 |

标准质量门于本地时间约 14:23 完成。本次不重新调用真实模型、不修改真实密钥、不重跑 Electron UI、不生成安装包。此前 UI、DMG 校验、真实 DeepSeek 与 Pi 认证结果仍以 [P10](desktop-app.md)、[P9](pi-codex-integration.md) 的日期记录为准。

## 留存的实现缺口

[Backlog](../implementation-backlog.md) 已登记：CI 缺少桌面依赖安装、完整计划 schema 尚未贯穿读取路径、长段落分块保真、CLI 导入取消未贯通、Pi 开发工具守卫的词法检查边界、数字开头主题的同步路由不一致，以及全局禁外发开关对 mock 材料调用的影响。这些都是文档核对发现的现存行为，本次没有把它们标成已修复。

Windows/Intel Mac、签名/公证、Pi 登录恢复、真实系统新密钥保存往返和桌面学习功能仍需各自验收。自动化测试数量不代表真实模型质量、覆盖率或全平台可用性。
