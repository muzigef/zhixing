# Kimi API 配置与验收（2026-09-09）

状态：工程开发、macOS arm64 实包验收及本机 0.9.1 更新完成；尚未填写或验证用户真实 Kimi Key。

## 范围

- 国内 Moonshot API `https://api.moonshot.cn/v1/chat/completions`、`kimi-k3`；快速/均衡/深入对应 low/high/max，始终开启思考。
- CLI 与桌面共用 AgentService、模型工厂和有界 Chat Completions 传输；工具执行、记忆/上下文和恢复策略不分叉。DeepSeek 保留原端点、模型选择与推理语义。
- `kimi-api` 会话/路由/设置/诊断标签；macOS CLI 隐藏输入配置 Keychain，桌面独立 `kimi.credential` 系统加密保存与可选 CLI Keychain 复用；不覆盖 DeepSeek。
- 设置页两家独立 Key 输入，切换清空未保存输入，主进程不回传 Key。两家均可点击测试连接；合成问题，无资料/历史/工具，2,048 输出预留、60 秒时限，只返回耗时和模型信息。
- 首次失败测试确认 Kimi 模块缺失、凭据原先固定 DeepSeek；随后实现并测试。独立团队模式仍未实施。

## 验证

- 根目录与 desktop `npm ci` 均退出 0，无新增依赖。
- 定向测试：7 文件 41 项全部通过；覆盖两家隔离、推理续接、原始工具参数、错误脱敏、离线开关、共享工厂、预算及连接探针。
- 最终 `CI=1 npm run verify` 退出 0：133 个测试文件、651 项测试；另跑 integration 9 项、eval 6 项及 mock smoke。lint、两端类型检查、敏感扫描与 diff 检查通过。
- 新增真实 CLI/桌面入口的长聊天、教学 Kimi 请求逐字段一致性检查，覆盖摘要、历史与工具 schema；使用合成 HTTP/Keychain。
- 开发应用六组 UI 退出 0；`npm --prefix desktop run pack` 退出 0，生成 0.9.1 macOS arm64 `.app`。最终样式在实际包六组 UI 中复测，退出 0，并查看设置页截图。
- 根目录与 desktop 生产依赖审计均退出 0、0 漏洞。默认 npm 镜像 audit endpoint 返回 400，改用单次 `--registry=https://registry.npmjs.org` 后完成；没有修改全局 registry。
- 9 份相关文档 221 个本地链接检查通过；包内 386 个输入文件哈希与最终源码一致。
- 最初一次 verify 的末尾扫描把合成 Authorization 字面量误判为疑似凭据。调整测试夹具表达方式后，最终完整门禁通过，没有放宽扫描规则。

## 本机安装

已正常退出旧版，原子替换 `~/Applications/知行.app` 为 0.9.1 并重新打开。应用数据目录未被安装脚本访问或修改；仅更新应用程序包。旧版程序保存在仓库的 `desktop/release/installed-backups/20260909-154243/知行.app`，该目录被 Git 忽略。

本机应用与已验收实包的 app.asar SHA-256 一致，`codesign --verify --deep --strict` 通过（ad-hoc）。来源 codeHash `2947bb18667cd9e02ab72308f326251161cbecbb206883bb50bf7e7c19c6e7c8`，基于 `47c121f48aef76d32d57300798c722f88f7868f0` 的未提交工作区。详细路径与校验和见 [安装回执](kimi-install-20260909.json)。本次未生成新版 DMG/ZIP，也没有执行 Git 提交或推送。

系统未授予 CUA 界面自动操作权限，配置页的最终 Key 输入/保存/测试由用户在本机操作；未尝试代填或读取密钥。

## 真实账户与边界

用户尚未通过本机入口保存 Kimi Key；没有读取、输出或写入任何真实凭据。真实 API HTTP 成功、响应耗时及原生 Kimi 密文保存尚未验收。UI 使用隔离合成凭据、模拟 HTTP/cipher，不能等同真实服务或 OS 加密验收。

本次不证明 Kimi 教学效果、三模型团队质量、其他操作系统或正式签名/公证。上一轮本机钥匙串记录只适用于该安装快照。

## 官方协议来源

- [Kimi K3 国内接入、推理、流式与工具续接](https://platform.kimi.com/docs/guide/kimi-k3-quickstart)
- [Kimi 官方模型仓库](https://github.com/MoonshotAI/Kimi-K3)

官方文档于 2026-09-09 核对；模型开通需平台账户充值，实际权限由真实请求确认。

## 本地验证日志

以下日志不包含真实凭据，临时目录文件不随 Git 提交。

| 日志 | SHA-256 |
| --- | --- |
| `/tmp/zhixing-kimi-verify.log` | `b69c837a82fa18728466d7f1a9d5a2f00bd1a46ae5858c808f4caff2b1971438` |
| `/tmp/zhixing-kimi-ui.log` | `60dd34869e8e0b8cb2960a1340a3b772df089f24991fcd1eb04c2aa9380968f9` |
| `/tmp/zhixing-kimi-pack.log` | `ba4299e301cbf997d7a46b6d8000fd3d3eb2e0493a34336dc9cb768223562e52` |
| `/tmp/zhixing-kimi-pack-ui.log` | `43fd94f93a61b86b37ad61cc16d7513789dab4fddea524cd595ebfaaeb1746a2` |
| `/tmp/zhixing-kimi-audit-root-official.log` | `6d8c5c8f3d7684adb070417bd608d01ae90aa3dc26a65af03ffda4955f38d9a3` |
| `/tmp/zhixing-kimi-audit-desktop-official.log` | `6d8c5c8f3d7684adb070417bd608d01ae90aa3dc26a65af03ffda4955f38d9a3` |

## 用户保存配置后的真实复验（2026-09-09）

用户在设置中配置 Kimi 后，使用已安装 0.9.1 的受控 `boot` / `check-api` IPC 复验成功。返回 `configured=true`、`source=desktop`、`model=kimi-k3`；真实合成探针首字 20,367 ms、总耗时 20,490 ms。此时间包含本机解密、网络和服务端处理，不等于纯模型推理时间。

验证脚本 `desktop/scripts/check-installed-api.mjs --live --provider=kimi-api` 不读取或输出凭据；只有应用主进程解密并向原 Kimi 端点发送固定测试消息，不携带对话、资料、工具，也不创建会话。日志 `/tmp/zhixing-kimi-live-20260909.log` 只含配置状态和耗时。此复验补齐上文当次安装时尚未配置的真实连接项，不证明教学效果或团队效果。
