# 三项 Agent P0 再次深入复核

2026-09-07。用户要求再次确认三项 P0 是否全部高质量完成。依据原[冻结标准](../p0-agent-kernel-plan.md)重新审查实际入口与故障边界，未新增 P0 目标。上一轮 383 测试通过是当时实际结果，但不能覆盖下述遗漏；本报告补充并修正[上一轮开发验收](p0-development-20260907.md)的完整性结论。

## 本次发现与修复

| 对应项 | 复现问题 | 修复与回归 |
| --- | --- | --- |
| P0-1 共享服务 | 第二个服务实例可重命名运行中的会话；快照可能覆盖运行状态。编辑上下文、撤回/恢复队列及后台整理存在同类未加锁路径 | 所有上述修改遵守共享会话租约；后台摘要在锁内核对最新消息再合并，避免覆盖另一入口的新消息/标题。队列启动前被另一入口接管时保留持久队列，不覆盖或产生未处理拒绝。agent-service 覆盖竞争修改、迟到摘要和执行交接 |
| P0-2 审批恢复 | 审批答复保存后，如果后续 Provider 初始化失败，最后消息仍为 waiting，但卡片已经 answered，界面没有继续按钮 | 加载时识别并提供 interrupted 继续入口；原 taskId 和审批决定保留；恢复只执行原调用一次。desktop-interactions 覆盖真实服务失败与重建；Electron 覆盖按钮可用及任务身份 |
| P0-2 交互身份 | 同一会话不同任务复用 Provider callId 时，第二张审批卡被第一张错误去重 | 按 taskId+callId 生成的稳定卡片 ID 去重。验证只批准第二项时，只保存第二项产物，第一项仍待授权 |
| P0-3 完成规则 | 模型删除未完成测试步骤，或将 tests_passed 改为 artifact_saved，可以移除原完成要求 | 禁止模型移除既有步骤或更改完成条件，允许新增、重排和标题调整；两个失败回归验证删除和降级都不会改变原条件 |
| P0-3 证据有效性 | 测试通过后修改实现，无论由 Agent 保存还是另一个入口保存，旧 completed 标志仍可让任务宣称完成 | 完成步骤绑定实际操作；完成检查异步核对当前实现/测试哈希、产物完整性和有效报告。两种修改路径均要求新验证，否则 blocked |

初次新增反例运行实际得到 5 项失败（删除/降级、两种代码变更、竞争改名）；两个审批反例随后分别先失败再修复。未跳过用例或改变原八项验收条件。另补跨实例后台摘要与 Electron 回复恢复回归。

## 已执行验证

- 定向运行 `npx vitest run tests/agent-p0-execution.test.ts tests/agent-service.test.ts tests/desktop-interactions.test.ts tests/agent-checkpoint.test.ts tests/agent-crash.test.ts tests/task-execution.test.ts`：6 文件 / 29 测试，退出 0，包含三个真实 SIGKILL 边界。
- `node --import tsx scripts/audit-agent-p0.ts`：原 8 项全部通过，退出 0。真实本地实验首次 exit 1，修复后 exit 0；审批继续、纠正和强杀恢复均保留任务及实际产物。
- `npm --prefix desktop run test:ui`：四组 Electron UI 全部通过，退出 0；新增已保存答复后的“继续回答”入口，核对原 taskId。
- 完整质量门禁首次停于新增测试的一处 prefer-const，修正后 `npm run verify` 退出 0：84 文件 / 390 测试、两套类型检查、lint、integration 9 项、eval 6 项、mock smoke、敏感扫描与 diff 检查。
- 最后补充队列执行交接反例，先复现 session_in_use 从后台收尾抛出，再修复；定向 agent-service 4 项通过。此后最终 `npm run verify` 退出 0：84 文件 / 391 测试，两套类型检查、lint、integration 9 项、eval 6 项、mock smoke、敏感扫描与 diff 检查全部通过。四组 Electron UI 也再次运行并退出 0。
- `node --import tsx scripts/verify-p0-live.ts --live --output=docs/evidence/p0-live-reaudit-20260907.json`：Pi Codex / gpt-5.6-terra 7,249 ms，DeepSeek / deepseek-v4-flash 1,745 ms，均退出 0。各 1 次初始请求、1 次原生工具续接、1 个实际产物、同一 taskId；SQLite 未保存 Provider 私有状态。[结构化记录](p0-live-reaudit-20260907.json)。这只是各一次合成恢复样本，不是延迟基准或开放任务质量测试。

## 结论边界

完成此次修复与最终验证后，三项 P0 在原冻结验收范围内均通过，本次没有发现仍未解决的范围内阻断项。新增 8 项自动化用例，并增强既有并发保护断言与 Electron 恢复覆盖。README、任务清单、内核说明和验收文档均同步到本报告。

本次结论仅针对三个冻结 P0 的执行语义与工程验收。模型是否正确理解开放式目标、测试本身是否充分、实际教学效果及商业 Agent 整体成功率，不由这组测试证明。Windows/Intel 实机、签名、公证和新安装包不在此次验收。没有提交或推送 Git。

后续用户再次要求深入核查，新增反例发现排队权限、CLI 取消、待执行调用纠正和循环状态判断的遗漏。上文保留为当轮 391 测试的实际记录，当前结论与修复见[再次契约核查](p0-contract-audit-20260907.md)。
