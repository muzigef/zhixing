# Agent P1 / P2 实施证据

基线 `b2874db`，范围见[冻结计划](../agent-p1-p2-plan.md)。各项记录实际命令结果，不将本地程序验收代替真实模型质量、独立评分、学习效果或外部平台验收。

## A1：上下文管理

- 修复前新增 8 项用例，7 项失败：极小截取预算超过上限、摘要后丢失显式纠正、旧工具转录使恢复直接超限、缺少输出 token 预留。随后补充中英混合上下文保护与不可变性断言，共 9 项。
- 增加 `modelContextWindow`：估算输入 token 并预留输出；裁剪完整旧执行轮次和普通旧消息，保留当前输入、目标/约束、最近回答、工具配对及待执行批次。持久转录不变，无法容纳的必要内容明确拒绝，不虚构已读取全文。
- CLI 普通回答/工具问答使用角色化历史；桌面摘要之外保留最近四次显式纠正（每次最多 4,000 字符），输入优先级不提升为系统权限。上下文计数保存在会话并有桌面可展开说明。token 为估算，非账单或精确 tokenizer。
- 定向上下文、恢复、工具协议、共享入口通过；角色化请求使 5 条旧 CLI 断言需从首条消息改为核对完整角色消息，内容、次数、主题隔离断言保留。实际 CLI 19 项通过。
- `npm run verify` 最终退出 0：87 文件 / 428 测试，lint、两套类型检查、integration 9、eval 6、mock smoke、扫描及 diff 通过。日志 `/tmp/zhixing-p1-a1-verify.log`。期间一次全量 Pi CLI 子进程超过原有 4 秒测试期限，单独 3 项通过，未改生产行为/期限；随后完整重跑通过，保留此环境波动记录。
- `npm --prefix desktop run test:ui` 退出 0，四组 UI 通过，新增实际窗口“本轮上下文范围”和估算说明断言。
- 此项未调用真实 Provider，不改变模型连接或计费设置；原始单体 prompt 中不可分离的必要内容超限时仍明确失败。后续条目继续按序实施。

## A2 — 调用效率

- `agent-efficiency.ts` 按 practice/skills/external/project 分类只向模型发送当前需要的 schema；`discover_tools` 可动态加载实际注册的定义。初始保留进度/检索与问题工具；任务已有计划或恢复历史已调用时直接恢复类别，避免再次发现。完整注册表仍验证权限，发现不授予写权限。
- 新增可选“自动”思考强度：短问答 quick，比较/设计 balanced，推导/执行 deep；用户显式档位优先。持久记录本轮实际档位，UI 显示自动选择结果。
- 测试先行：初始 4 失败/1 通过；最终包含 6 项新测试，验证真实产物写入必须审批、服务重启审批恢复、无资料授权无发现入口、自动选择与显式覆盖。桌面实际选择自动并生成数学回答、显示深入档位。
- `npm run verify`：退出 0；`npm --prefix desktop run test:ui`：退出 0，四组实际 Electron 验证通过。完整日志 `/tmp/zhixing-p1-a2-verify.log`、`/tmp/zhixing-p1-a2-ui.log`。
- 本项确定减少初始无关工具定义；发现本身可能增加一轮模型往返，不能直接宣称所有任务更快。真实模型对照放到 A4 的统一留出评估。未增加常驻 Pi 进程；现有独立 worker 保持取消与隔离语义，连接复用需要实际时间证据。

## A3 — 回答呈现

- 新增 `ContinuationText`，在明确“继续回答”且上一轮中断时，流式省略完全相同的长开头。过滤仅作用于展示，原始 Provider 执行记录保留；少于 40 字符、超过 6000 字符、含代码/公式的原段落不剪裁。部分重合、取消和结束都释放缓冲，防止丢字。非逐字重复仍依赖模型提示，不能宣称消除所有语义重复。
- 新增格式/引用观察并持久展示：围栏或公式块未闭合、裸数学命令、无匹配引用的编号、明确段落要求偏差。提示不是事实评分，不自动改写公式或代码，也不把提示存在当作回答错误结论。
- 先运行新增测试确认缺失模块失败，随后 4 项新测试通过，包含真实 AgentService 的 delta/最终内容/item/重启一致性；桌面实际展示格式提示与数学渲染。
- `npm run verify`：最终退出 0，89 文件 / 438 测试；四组 `test:ui` 退出 0。全量测试两次遇到已有 Pi CLI 4 秒子进程超时，单独运行 3 项通过；新增 `vitest.config.ts` 将全套并行 worker 限为 4，防止多组 CLI 子进程同时启动造成资源争抢，未修改取消/超时断言、未跳过任何测试。随后全量门禁通过。
- 真实合成新题与续写样本将在 A4 统一记录。独立人工复核尚未进行。

## A4 — 留出评测与实际失败闭环（持续记录）

- 新增冻结六题留出集、逐项评分导入、排序键规范化的报告哈希绑定、重复/过期/不完整评分拒绝、逐条件 P50/P95 与清晰分母。异常脱敏，未发出请求不复制耗时；新运行拒绝覆盖旧报告。新增实际 CLI 导入与拒绝覆盖测试。
- 先确认缺少评分模块失败；初次集成发现 Zod 解析改变对象键序影响哈希，改为对象键排序、保留数组顺序，真实 CLI 回归通过。`npm run verify` 通过 443 测试；实际 demo 四次完成，统计显示评分数 0、通过率 null。
- 首轮真实合成评估：Pi Codex 使用 Pi 配置的 `gpt-5.6-terra`，DeepSeek 为 `deepseek-v4-flash`；四题各两次，共 16 次均完成。开发助手复核为 9 通过 / 16 评分，独立人工评分为 0。发现 DeepSeek 段落超限、语义重讲、公式排版和来源结论过强。
- 仅提高 balanced 档位的八次对照：5 通过 / 8 评分（开发助手），DeepSeek 仍两次重讲检索；另有把昨天的天气当缓存正确例子的事实错误。证据不支持“提高思考强度就能解决交互质量”。保持自动策略与显式覆盖，没有强制提高用户档位。
- 针对明确段落/续写请求补上靠近当前输入的动态系统约束，保持实际用户消息原文不变。CLI 原生角色消息也接入。新增对独立引用标记的匹配检测、公式边界排版提示。修复后的 16 次回归中开发助手复核 11 通过 / 16；这已是回归集，不再宣称未见题。
- 该回归另发现一次只返回 `continue`，新增原生可续接模型的有界回答修复：至多追加一轮，持续不合要求则 blocked；明确段落数也可触发同一门禁，不执行额外写入。完整单行公式块完成后规范开闭换行，保留公式字节，不改代码围栏、不补造未闭合公式，原始 Provider 轮次保留。新增测试先失败后通过；最终完整 verify 通过，实包前还会整体复验。
- Pi 八次初始 worker 启动约 0.51 秒（首次 0.955 秒），模型请求约 4.3–14.3 秒，进程尾部约 0.35 秒。常驻连接的潜在收益有限，不足以抵消取消/账号隔离复杂度，本轮未强加常驻 worker。首次修复回归的前几个样本与 Electron 构建/测试重叠，不作严谨耗时提升对照。
- 原始、评分与可复算汇总保存在 `agent-heldout-{auto,balanced,refined}-*20260907.json`。流程使用合成材料，不读取用户资料或凭据；独立人工评分与真实学习者效果仍待实际参与。

### A2–A4 最终验收记录

最终 `npm run verify` 退出 0：91 文件 / 447 测试，集成 9、Eval 6、mock smoke、扫描、根/桌面类型检查与 lint 通过；四组 Electron UI 退出 0。日志 `/tmp/zhixing-p1-a4-final-verify.log`、`/tmp/zhixing-p1-a4-final-ui.log`。

最后以修复过的 H02/H04 回归和此前未运行的 H05/H06 新题各两次、两个 Provider 运行 16 次，均完成。开发助手评分 13 通过 / 16、3 部分，独立人工评分 0。公式块最终格式、导数验算以及 H06 实际工具次数通过；DeepSeek 仍有衔接冗余、无必要资料查询和过强一致性建议。不能据此声称商业 Agent 同等质量或全题正确。全部四组共 56 次真实请求；报告完整保存失败与部分结果，没有为了提高分数改题或抹去旧报告。

最后报告为 `agent-quality-final-20260907.json`，逐项评分、汇总分别见同名前缀的 review/summary 文件。H06 两种 Provider 各两次均只有一次资料工具调用、两轮模型请求、没有写入。初始工具定义的字符数对照见 `agent-schema-measure-20260907.json`；不是实际 Token 计费。A5 起继续验证应用层学习闭环，真实回答的剩余局限保留，不阻止有明确边界的其他工程增量。

## A5 — 学习观察与人工解释复核

- 学习观察只从真实已提交的知识检查生成，原始作答、帮助方式、来源 ID、日期和反思保留。修改附注使用版本冲突检查并留历史；可撤回/恢复。只在相关学习问题且已允许当前主题上下文时加入最近三条有效观察。模型没有修改观察或人工评分的工具；撤回阻止未来注入，不删除此前对话。
- 帮助方式分为独立、提示、直接答案、未说明；使用提示或答案即使选择题全对也安排一天后复习，不沿用独立连续通过的七天间隔。老记录的未说明状态兼容保留，不推定为独立完成。
- 教学对照的解释复核只在试验结束或放弃后开放，绑定当时的回答和版本，保留来源哈希、署名、理由及撤回历史。选择题得分和对照结果不会被人工意见改写，也不会把对照答案泄漏回教学上下文。
- 初始两项测试失败后完成实现；追加帮助间隔与备份边界测试分别暴露真实缺口。修复备份目标父目录经符号链接进入工作区的绕过，使用实际路径重新核对。最终新增三项集成测试覆盖重启、主题隔离、冲突、撤回和完整备份恢复；定向八项通过。
- 最终 `npm run verify` 退出 0：92 文件 / 450 测试、integration 9、eval 6，全部检查通过。四组 Electron UI 退出 0，实际操作帮助选择、编辑/撤回观察、人工复核及撤回并检查持久结果。日志 `/tmp/zhixing-p1-a5-verify.log`、`/tmp/zhixing-p1-a5-ui.log`。
- 本项使用合成测试作答；人工身份为用户署名自报，未验证真实独立评审者或教学效果。

## B1 — 有界并行与流式渲染

- 只给三种独立学习查询显式 `parallelSafe` 标记；注册器拒绝写入或非幂等工具声明并行。每批最多两个相邻查询，交互、计划、状态更新与写入保持屏障。共享桌面和 CLI 入口都接入。
- 首次并行时升级执行检查点为 v2，兼容读取 v1；持久记录全部已启动范围。按模型顺序保存真实结果，在通知可取消的消费者前保存整组已完成结果。恢复重新检查每个未知调用的重放权限；纠正分别记录已启动结果未知与尚未启动，不能冒充成功。
- 新测试先出现四失败、一通过及缺失批处理模块；完成后六项并行/回答修复恢复测试与一项批处理边界测试通过。覆盖并发峰值、写屏障、取消后不重做已完成结果、权限变化、双调用纠正、超限前零执行及回答修复次数跨重启不重置。
- 流式片段按约 32ms 合并，完整会话快照覆盖旧缓冲、结束刷新末段、会话切换清理缓冲。Markdown 仅文字变化时重解析；修复旧 Message 自定义 memo 忽略新操作回调的问题。
- `npm run verify` 退出 0：94 文件 / 457 测试，集成 9、eval 6 及所有门禁通过。四组 Electron UI 通过；真实 IPC 1001 片段检查最终字节、复制、快照覆盖与会话隔离，含断言耗时 50ms。日志 `/tmp/zhixing-p2-b1-verify.log`、`/tmp/zhixing-p2-b1-ui.log`。
- `node --import tsx scripts/measure-agent-performance.ts` 的五组交替合成对照保存在 `agent-performance-20260907.json`：四个 40ms 查询，串行 168–170ms、并行 84–85ms；峰值分别 1 和 2，结果顺序完全一致。1000 个连续片段合成一次更新。该结果证明应用层调度收益，不代表真实 Provider 请求减半或普遍交互延迟。

## B2 — 受控 MCP

- 新增主题配置、双代 stdio 协议、分页能力发现、JSON Schema 工作线程验证、最小环境、取消/超时及进程清理，接入共享 Agent 的按需发现与具体审批。桌面可保存、测试及停用；未配置服务默认不连接。详细范围及官方依据见 [使用指南](../mcp-tools.md)。
- 配置只允许显式列出的工具，服务器的只读注解不授予权限。工具标识绑定连接参数、权限和输入 schema；改配置无法复用旧审批。备份恢复停用连接。单个服务失效提供状态工具，普通查询继续。
- 新增测试先因缺失模块失败，实际子进程协议/Agent 入口六项通过。后追加 schema、断连写入、超时竞争与离线隔离用例；超时竞争和离线隔离各先失败一次后修复。非幂等外部写入只要可能开始但结果未知，就抛出恢复要求，保留执行检查点，不能按普通失败自动重做。
- 最终 `npm run verify` 退出 0：95 文件 / 467 测试、integration 9、eval 6 及全部检查。四组 Electron UI 退出 0，实际窗口配置临时协议服务、校验工具列表并停用；生产 UI 未增加测试后门。日志 `/tmp/zhixing-p2-b2-verify.log`、`/tmp/zhixing-p2-b2-ui.log`。检查完成后没有遗留夹具服务进程。
- 本轮新增固定 Ajv 8.20.0 / ajv-formats 3.0.1，根目录与桌面锁文件同步。首次 verify 报夹具缺少 Node process 导入，补上显式导入后完整通过；未放宽 lint。
- 验证对象为真实运行的本地合成协议服务，不等于已连接 GitHub 等第三方生产服务。服务自身以用户权限运行；不宣称拥有 OS 隔离、MCP HTTP/OAuth、非文本内容或完整 MCP 生态兼容性。

## B3 — 独立实践项目（2026-09-08）

- `PracticeProjects` 提供受主题和用户选择约束的项目文件、独立裸 Git 仓库、差异、真实测试、检查点与租约。导入只复制允许文本，不复制源 `.git`、不更改源目录。文件写入绑定原哈希，测试和检查点绑定整棵文件树；修改使旧验证失效。完整备份保留项目与 Git 历史，恢复清除选择和授权。具体容量及执行边界见 [项目指南](../practice-projects.md)。
- 共享 Agent 接入项目发现、分页读取、差异审批和三种实际完成条件。桌面支持创建/导入、选择、编辑预览、失败/通过测试、Git 历史；CLI 同样显示具体差异后才接受审批。
- 初始项目测试因缺少实现失败，逐步完成九项集成回归；新增分页边界先发现结果被通用输出上限截断，再修复分页和哈希保留；新增实际 CLI 进程测试先发现审批只显示标题，修复为显示差异，拒绝后无文件改动。未增加跳过项或放宽 lint。
- 第一次真实 Pi 项目请求 24.161 秒失败，未改文件；诊断复跑 66.587 秒仍未改文件，保留完整工具记录。真实模型填写了课程 `kind` 字段，而项目计划拒绝该字段；工具 schema 没有表达这个条件，且只返回泛化错误。另确认第七轮计时会超过旧会话 schema 的六条上限。
- 测试先行增加计划发现/schema 与迁移边界，出现五项失败、十项通过；随后用条件分支 schema 区分项目/课程计划，项目发现同时返回计划定义，不加载无关课程写工具；反馈明确计划错误码。会话 v4 兼容旧版备份，允许十二轮计时。三文件 21 项定向回归通过；没有通过提高调用时限或关闭防循环规避问题。
- 最终 `npm run verify` 退出 0：96 文件 / 480 测试、integration 9、eval 6、mock smoke、两套类型检查、lint、敏感扫描与 diff 检查。五组开发 Electron UI 退出 0。日志 `/tmp/zhixing-p2-b3-final-verify.log`、`/tmp/zhixing-p2-b3-final-ui.log`；新增项目 UI 验证失败 → 修正 → 通过 → Git 检查点、重启及主题/选择隔离。

同一临时合成任务要求只追加 README 一行、保留原文和其他文件、建立三步计划、运行实际 `.test.mjs`、保存 Git 检查点。通过现有 Pi 产品接口和 DeepSeek Keychain 接口调用，不读取认证文件，也不发送真实项目资料：

| Provider / 实际模型 | 总耗时 | 模型回合 / 工具次数 | 结果 |
| --- | --- | --- | --- |
| Pi Codex / gpt-5.6-terra | 40.054 秒 | 8 / 8 | 六项文件/完成检查全通过，计划完成 |
| DeepSeek / deepseek-v4-flash | 9.900 秒 | 7 / 7 | 六项文件/完成检查全通过，计划完成 |

报告：[Pi 原失败](project-live-pi-20260908.json)、[Pi 诊断失败](project-live-pi-diagnostic-20260908.json)、[Pi 修复后](project-live-pi-fixed-20260908.json)、[DeepSeek](project-live-deepseek-20260908.json)。后两份保存前后 README、工具记录和实际文件/Git/测试快照。该单任务结果不是普遍速度或复杂项目成功率；普通质量集中的残余语义问题仍保留。

## B4 — 0.5.0 本机交付（2026-09-08）

- 桌面版本及锁文件同步为 0.5.0；release 工作流 macOS 构建前增加完整 verify，保留各平台开发/实际包 UI。更新 README、桌面文档、架构、配置、开发、测试、任务和学习效果指南，新增 0.5/项目/MCP/质量评测文档，历史证据保持当轮含义。
- 两套 `npm audit --omit=dev --audit-level=high` 初次均因本机镜像审计接口 HTTP 400 失败；仅当次指定 `--registry=https://registry.npmjs.org` 后均退出 0，**0 vulnerabilities**。没有更改全局 registry。日志 `/tmp/zhixing-p2-audit-{root,desktop}-official.log`。
- `CSC_IDENTITY_AUTO_DISCOVERY=false npm --prefix desktop run dist:mac` 退出 0，生成 arm64 DMG/ZIP。打包日志 `/tmp/zhixing-p2-package.log`；重复依赖提示属于打包器信息，未将其隐藏。未使用 Developer ID 签名或公证。
- 使用实际 `desktop/release/mac-arm64/知行.app/Contents/MacOS/知行` 设置 `ZHIXING_DESKTOP_EXECUTABLE` 后运行五组 `test:ui`，退出 0；覆盖内附运行时、MCP schema worker、学习观察/复核、项目实际失败/通过测试和 Git 检查点。1001 个 IPC 片段的末段、复制和会话隔离断言通过，包含 UI 断言为 51ms；这不是模型延迟。日志 `/tmp/zhixing-p2-packaged-ui.log`。查看实际截图，聊天排版、复制控件与输入区域无重叠。
- `hdiutil verify` 退出 0、checksum VALID；只读挂载后核验版本 0.5.0、最低 macOS 13.0、arm64、Applications 链接；DMG 内可执行文件和 `app.asar` 的 SHA-256 与实际测试 `.app` 一致，之后卸载临时挂载点。
- `npm --prefix desktop run checksums` 退出 0；`shasum -a 256 -c SHA256SUMS-darwin-arm64.txt` 的 DMG/ZIP 均 OK。产物校验信息另存 [delivery-0.5.0-20260908.json](delivery-0.5.0-20260908.json)。安装器在被 Git 忽略的 `desktop/release/` 中，不随源码提交，不自动安装或正式发布。
- GitHub CLI 只读确认基线 `b2874dbff92688d06599814468bb26d020a2d2a4` 的 [verify 34121979925](https://github.com/muzigef/zhixing/actions/runs/34121979925) 成功。当前未提交改动没有远端 CI 结果；本轮未提交、推送、触发发布。
- 当前可验收的九项工程范围已完成。真实学习效果/独立人工评审、具体第三方 MCP 服务、Windows/Intel 实机、签名/公证仍是外部验收项，不能用本机自动化替代。

文档收口的第一轮 480 项测试全部通过，但 diff 检查发现历史 0.4 文档末尾多余空行而退出 2（`/tmp/zhixing-p1-p2-final-verify.log`）；修正空白后重新执行完整质量门，退出 0：96 文件 / 480 测试、integration 9、eval 6、mock smoke、两套类型检查、lint、敏感扫描和 diff 检查全部通过；最终日志为 `/tmp/zhixing-p1-p2-final-verify-2.log`。17 份本轮 Markdown 的本地链接目标均存在。所有普通测试使用临时合成数据；现有用户资料、原项目和凭据未被作为测试夹具。
