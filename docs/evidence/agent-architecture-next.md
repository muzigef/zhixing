# 架构与教学增量执行记录

基线 `a1ba9ac`，2026-09-08 开始。范围见 [执行计划](../agent-architecture-next-plan.md)。

## 基线

- 工作区干净；上一轮 480 项测试记录保留，不作为本轮重跑结果。
- 当前提交的 GitHub verify `34179197671` 在 npm ci 阶段因私有 registry 的域名解析失败退出，后续测试未运行。
- 审查用本地合成数据复现 MCP 写后重读误判、参数诊断丢失、自定义调用统计丢失；另确认未知外部写入安全停止后缺少显式核对入口、合法引用不保证结论受证据支持。

## 执行

### C01 — 已复现缺陷

- 新增 5 条回归先全部失败；修复后四文件 40 项通过。MCP 资源版本来自持久执行结果，修改后的读取允许，第三次无变化读取仍被拦截。参数错误保留字段路径/类型，不回显输入；自定义调用合并用量、耗时和审计回调。
- 两份锁文件共修复 14 个下载地址，版本与 integrity 不变。新增纯 Node 地址门禁，在 verify 与发布 CI 安装之前运行。
- 全新临时 npm cache、显式公共 registry，根目录/desktop 两次 npm ci 成功；日志 `/tmp/zhixing-c01-install.log`。
- `npm run verify` 退出 0：97 文件 / 485 项测试、integration 9、eval 6、mock smoke、两套类型检查、lint 和静态检查。日志 `/tmp/zhixing-c01-verify.log`。
- `npm --prefix desktop run test:ui` 五组退出 0，日志 `/tmp/zhixing-c01-ui.log`。本轮尚未触发新的远端 CI。

### C02 — 工具契约与完整结果

- 使用现有 Zod 包的 v4 公共入口和原生 JSON Schema 导出，锁定兼容包版本 3.25.76。统一应用 schema 导入，模型输入定义直接来自工具执行 schema；保留历史任务快照宽格式，新计划用条件联合 schema。未新增已停止维护的转换依赖。
- 工具描述、输入、风险、资源与执行集中注册。输出截断保留状态/退出码/哈希；大结果按任务和主题保存，可经 read_tool_result 分页读取，重启后有效。容量不足明确标记不可续读，不伪造操作失败。
- 新测试先失败；五文件 25 项定向通过。全量 verify 两次退出 0：99 文件 / 489 项，integration 9、eval 6、mock smoke、lint、两套类型检查与静态检查。最终日志 `/tmp/zhixing-c02-verify-2.log`。
- 首次 UI 在资料导入后遇到弹窗意外关闭；原用例独立复跑通过，但新增“内部按下/外部松开”确定性用例失败。修复背景关闭的完整指针手势判断后，五组 UI 通过，`/tmp/zhixing-c02-ui-2.log`。不是放宽超时或删除断言。
- JSON Schema 生成依据 [Zod 官方文档](https://zod.dev/json-schema)，使用输入视图并保留不能表达约束时的失败行为。真实 Provider 暂未在本轮调用。

### C03 — 恢复、目标修订与累计用量

- 新增 TaskContinuity 与受控 MCP 只读核对适配器，任务详情及 `/task` 共用服务接口。操作标识和配置/schema 必须一致；未知结果不重放。用户自报保持 `verified: false`，同工具后续写入即便有会话权限也重新逐项批准。
- 真实本地 MCP 子进程夹具覆盖写入后断连、已执行/未执行、身份错误、未知状态、配置撤回与自报后重新审批，均只发生一次原始写入。十项连续性测试加 CLI 两项，共 12 项定向通过；未连接指定之外的第三方服务。
- 显式目标修订使用版本比较并归档旧计划，保留同一 taskId 和既有操作。每段执行用量、模型/工具请求数及停止原因在 SQLite 累加；未报告 Token 不估作实际计费。
- `npm run verify` 最终退出 0：100 文件 / 499 项测试、integration 9、eval 6、mock smoke、两套类型检查、lint 与静态检查，日志 `/tmp/zhixing-c03-verify-2.log`。
- 五组桌面 UI 退出 0，含任务详情修订/同任务续接/累计用量，日志 `/tmp/zhixing-c03-ui-2.log`。首次 UI 在新执行段结束前读取累计计数导致失败；改为等待实际执行完成，并让面板随执行状态刷新，不放宽业务断言。
- 保持既有每段预算。服务核对依赖服务自身状态契约，最终一致的暂未查到不能配置为明确未执行；说明已写入 MCP 文档与界面。

### C04 — 知识点证据与教学建议

- 24 个作者维护的知识点映射到现有题目；新作答逐题保存目录版本与结果，旧总分记录不反推概念分数。TeachingPolicy 区分错误、借助提示、帮助未知、用户更正和自报独立答对；建议带检查 ID、题号、修订和时间。
- 中英文概念相关性替代人称关键词。只读 learning_state、CLI 上下文和桌面共享策略；直接答案/完整推导与仅提示请求优先。UI 展示逐题知识点；更正及撤回影响新建议，历史记录保留。说明见 [教学策略](../teaching-policy.md)。
- 新测试先因缺少策略模块失败；四文件九项定向通过。完整 verify 退出 0：101 文件 / 502 项、integration 9、eval 6、mock smoke、lint 和两套类型检查，`/tmp/zhixing-c04-verify.log`。五组桌面 UI 退出 0，`/tmp/zhixing-c04-ui.log`。
- 规则及合成测试证明数据流和建议选择，不证明真实教学效果；独立解释评分、延迟复习与真实产品对照留在 C12 外部验收中。

### C05 — 来源版本与支持检查

- 检索路径统一携带实际片段字节的 SHA-256，打开来源拒绝已改变的内容；旧引用继续兼容。支持报告保存回答片段与检索范围/哈希，独立于格式诊断。
- 确定性检查覆盖未支持数值、无紧邻引用的测量、跨成本/命中率错用数字、过强措辞、长引语与同名来源歧义。资料问答拒绝这些结果；共享工具 Agent 最多修正一次，失败保持受阻，界面不将规则通过称为事实正确。
- 新增六项支持/来源/服务测试。初始测试夹具存在围栏转义语法错误，修正后验证了真实输入输出；不得将该语法失败计作功能复现。此前审查已复现合法引用下虚构 90% 成本降幅；本轮相同案例被拒绝。三文件十九项定向通过，另一次六项支持测试通过。
- 完整 verify 退出 0：102 文件 / 508 项、integration 9、eval 6、mock smoke、lint 和两套类型检查，`/tmp/zhixing-c05-verify.log`。五组桌面 UI 退出 0，`/tmp/zhixing-c05-ui.log`。
- 这是有限风险诊断，不是通用语义/数学/代码正确性证明。漏检范围及不可替代的验证详见 [支持检查说明](../evidence-support.md)。

### C06 — 模型能力、预算与可读取历史

- 显式描述适配器的文字/工具/续轮/输出配置能力；图片请求在调用前拒绝，桌面图片粘贴/附件拖入给明确提示。48,000 是应用策略，不宣称为商业模型规格。桌面及公开 CLI 环境项可降低预算，Pi worker 与 DeepSeek 的真实请求携带输出限制。
- 每段按 Provider 报告保守校准后续输入估算，不提高额度。审查安装的 Pi SDK `openai-responses-shared.js` 确认其 input 排除缓存；新增失败回归并统一输入为非缓存+缓存读取+缓存写入，避免与 DeepSeek 总输入口径不一致。
- 摘要来源范围、带消息 ID 的用户纠正及尚未解决任务进入结构化观察。分页工具读取本会话/任务原文，续页需哈希；拒绝模型指定其他会话与撤回资料授权下的执行历史。续接模型视图替换旧观察，原日志不改写。
- 新增能力/历史测试先失败；最终四文件十九项定向通过。完整 verify 退出 0：104 文件 / 515 项、integration 9、eval 6、mock smoke、lint、两套类型检查，`/tmp/zhixing-c06-verify.log`。五组桌面 UI 退出 0，含预算保存与不支持图片提示，`/tmp/zhixing-c06-ui.log`。
- 估算仍不是 tokenizer 或费用计算，校准当前限定执行段；CLI Pi 保持仅文本及原输出预留。详见 [模型与上下文](../model-context.md)。

### C07 — 按需外部连接与可见降级

- 共享 Runtime 只在 external 发现或相关任务恢复时加载 MCP 目录。目录按主题/配置修订/哈希缓存五分钟；命中时零服务启动，真正校验/执行前核对实时完整 schema。过期、配置变化和 schema 漂移不会沿用旧调用权限。
- 冷发现最多同时初始化既有四服务配置，保留单连接八秒与整个调用预算；业务 MCP 工具不并行。任务结束/等待审批关闭所有已打开连接，不新增常驻进程。原低层 eager 连接测试入口保留。
- 语义服务不可用或索引为空时提供关键词结果和明确原因，模型观察与桌面任务活动均可见；取消不降级继续。
- 新测试先因缺少入口失败；四文件三十项定向通过，补充缓存过期/撤回与实际服务活动后五项通过。完整 verify 退出 0：105 文件 / 520 项、integration 9、eval 6、mock smoke、lint、两套类型检查，`/tmp/zhixing-c07-verify.log`。五组桌面 UI 退出 0，`/tmp/zhixing-c07-ui.log`。
- 本地子进程记录证明普通问答零启动、缓存命中零额外发现、实际调用才新建连接；不是通过假计时推断提速。第三方服务实测仍待指定服务。

### C08 — 独立访问与可撤回操作授权

- 资料、选定项目和 MCP 配置分别绑定会话；旧会话兼容迁移后固定范围。写入卡只记住具体文件/操作或同版本外部工具的相同参数，学习操作下拉框不扩大为项目/MCP 写入。桌面、CLI、队列、执行历史、恢复及备份共享约束。详见 [会话授权](../session-permissions.md)。
- 新增授权测试先因缺少模块失败。验证独立工具暴露、换项目/配置拒绝、同文件复用而另一文件重新批准、撤回待批操作、分支和备份清除授权。队列既保留排队时的限制，也不能恢复后来撤回的权限。
- 首次全量发现原 MCP/项目测试依赖广泛学习写入授权，已改为真实卡片批准后继续原验证；队列限制回归在定向测试中发现并修正。桌面发现异步开关反馈迟缓，改为即时反馈、失败回退和保存中防重复。新增项目后备份测试需等待项目面板读取完成，已按实际界面状态等待。
- 完整 verify 退出 0：106 文件 / 527 项、integration 9、eval 6、mock smoke、lint、两套类型检查，`/tmp/zhixing-c08-verify-3.log`。五组桌面 UI 全部退出 0，`/tmp/zhixing-c08-ui-final.log`；包含三个独立开关、项目权限持久化/撤回与备份恢复。

### C09 — 可恢复项目修改与 Python 实测

- 修改前快照（最近 20 次）、哈希绑定的预览恢复、三行上下文差异、唯一片段修改及最多十文件批次。完整暂存/目录切换有持久事务；真实子进程在切换中退出后自动恢复原文件。发现额外变化时保留现场，不覆盖未知修改。局部修改/恢复增加持久回执，避免外层回执丢失后重复修改。
- JavaScript/Python 模板和真实 unittest Runner 已接入桌面与工具。总预算维持 10 秒，标准库隔离、不安装依赖；实际验证通过、失败以及沙箱外合成文件读取被拒绝。初始启动发现 Xcode Python 包装器需要再次执行及随机设备读取，改为直接解释器和明确系统随机设备读取，未开放工作区/网络。
- 六项新回归先因缺少模块失败，最终全部通过；另有十项既有项目测试。完整 verify 退出 0：107 文件 / 533 项、integration 9、eval 6、mock smoke、lint、两套类型检查，`/tmp/zhixing-c09-verify.log`。五组桌面 UI 退出 0，含实际快照恢复、失效测试记录和 Python 模板运行，`/tmp/zhixing-c09-ui.log`。
- 限制和恢复语义见 [项目修改与恢复](../project-revisions.md)。Windows/Intel 环境未在此声称通过。

### C10 — 历史索引、分页与增量渲染

- 可重建的纯元数据索引按文件 inode/大小/mtime/ctime 失效，正文 JSON 继续作为权威记录。损坏索引不损坏会话；新实例复用索引时不解析未变化正文。页面默认 40 项，按时间与 ID 游标排序，搜索覆盖全部元数据。已知未变化版本的频繁保存不再重复读取旧正文。
- 活动/卡片/阶段文本改为单消息补丁，桌面 IPC 16 毫秒合并连续片段并在元数据/结束事件前刷新；序号拒绝旧补丁。旧消息每次展开 40 条，保留全部持久消息和导出/恢复逻辑。
- 三项新测试先失败，最终通过。完整 verify 退出 0：108 文件 / 536 项、integration 9、eval 6、mock smoke、lint、两套类型检查，`/tmp/zhixing-c10-verify.log`。五组桌面 UI 退出 0，`/tmp/zhixing-c10-ui-2.log`；包含 1001 条真实 IPC、旧补丁和会话隔离、65 段历史的全局搜索/分页、100 条消息分批显示。初始新 UI 测试读取了异步切换前的消息数量，已等待真实消息页出现后断言。
- [本机规模测量](session-scaling-20260908.json)：500 段会话 / 20000 条消息 / 29,149,890 字节正文，冷索引 174.05 ms，五次热读取中位数 25.41 ms，热正文读取 0。索引 139,627 字节，每页 40 项。结果只描述这台 darwin-arm64/Node 24.8.0 的合成数据，不代表模型响应或其他设备。复跑：`npx tsx scripts/benchmark-sessions.ts`。
- 索引仅加速列表，目录扫描仍随会话数量增长；正文仍是有界 JSON，全量迁移数据库和全文搜索未在本轮扩大范围。

### C11 — 可追溯的 Skill 参考流程

- YAML 元数据增加声明版本、适用条件和关联评测，实际内容另有 SHA-256；即使未提升声明版本，文件变化也产生新哈希。重复 YAML 键和无效元数据被拒绝。默认技能附带适用条件和真实评测引用，关联不表示通过。
- 目录刷新失败时保留上次有效版本并明确标记 stale，模型结果、活动和桌面预览均可见；新进程无有效缓存时失败关闭。分段读取每页 5000 字符，续页校验版本。现有用户技能不被默认模板覆盖，旧版无声明版本仍显示实际内容哈希。
- 新版本/回退测试先失败；最终五项目录测试通过。完整 verify 退出 0：108 文件 / 537 项、integration 9、eval 6、mock smoke、lint、两套类型检查，`/tmp/zhixing-c11-verify-2.log`。初次 lint 发现解构未使用字段，已改成共享元数据投影函数。
- 五组桌面 UI 退出 0，`/tmp/zhixing-c11-ui-3.log`；实际修改合成技能为坏文件后显示旧缓存，恢复有效文件后更新。一次流式回归定位到异步渲染期间选中了更早回答的禁用复制按钮，测试改为明确定位最后一条回答的按钮，保留精确最终文本断言。

### C12 — 组合回归、完整产品协议与可追溯交付

- 新增双 Provider 原生工具契约测试：真实 wire 格式包含统一 schema、调用 ID 和输出预算，错误参数经下一轮纠正后才能执行；不完整流中的工具零执行。补充已批准但尚未执行时撤权、撤回后重新授予仍需新批准、错误工作区绑定以及批量写入不能复用成删除的组合边界。保留未知/执行中操作事实，撤权不伪装成回滚。
- 四主题二十四知识点新增另一组情境，共四十八道课程检查题。下发时持久保存题目、题库版本和题卷，原作答按原题评分，保留帮助声明。效果试验原有两主题、三份情境卷不混入课程题库；题卷难度尚未校准。
- `full_product` 知行组接入正常 Runtime、教学状态和获授权工具；直接聊天组保持隔离。旧 `prompt_only` 记录按原协议解释；两者都不自动把效果检查题/作答/解析发给模型。每轮记录真实模型、预算、权限、工具次数和构建来源，汇总按协议/构建分开，缺失或变动条件不混入完整产品对照。
- 构建来源覆盖 330 个核心、界面、worker、依赖、脚本、Skill、主题和评测文件。浏览器仅导入纯契约模块，主机文件读取与哈希留在宿主模块。桌面会话格式升为 v5、SQLite 标记升为 5；测试验证旧格式只读兼容、首次保存备份、旧应用拒绝新格式和恢复后的权限清除。没有打开或迁移真实用户工作区。
- 新增产品验证 6 项、双 Provider 契约 4 项，并扩展权限、项目、迁移等边界，最终 `npm run verify` 退出 0：110 文件 / 553 项测试、integration 9、eval 6、mock smoke、lint、根目录/桌面类型检查、公共 registry 和敏感信息静态检查。日志 `/tmp/zhixing-c12-verify-complete.log`。Python 新覆盖网络拒绝和真实超时；不是只验证 Runner 可以启动。
- 开发版本五组 UI 退出 0：`/tmp/zhixing-c12-ui-1.log`。首次实包 UI 前四组通过，项目组在第二次恢复 IPC 尚未结束时关闭应用，触发正确的取消行为；脚本改为等待恢复完成并断言实际差异，再关闭重启。单组实包复核及最终实包五组全部退出 0，最终日志 `/tmp/zhixing-c12-packaged-ui-complete.log`。未删除恢复断言或提高业务超时。最终课程/对话截图经目视检查，无遮挡或溢出。

#### 真实 Provider 验证与保留的失败

所有请求通过已配置的产品 Provider，使用临时合成主题、资料和 Git 项目，不读取凭据或发送用户资料。实际模型分别为 Pi Codex `gpt-5.6-terra` 与 DeepSeek `deepseek-v4-flash`；不把配置名当成实际响应模型。

| 验证 | 实际结果 | 原始记录 |
| --- | --- | --- |
| 两家模型项目执行（最终源码） | 各 6/6 检查：任务完成、保留 README、写入指定标记、其他文件未改、真实测试通过、新 Git 检查点；三次限定操作批准。Pi 总任务 41.749 秒，DeepSeek 30.562 秒；总耗时包含所有续接段 | [Pi](agent-architecture-project-pi-final-20260908.json)、[DeepSeek](agent-architecture-project-deepseek-final-20260908.json) |
| Pi 完整产品协议 | 直接聊天 / 知行组均完成教学对话并进入学后检查；7.464 / 11.645 秒。该快照在最后 schema 修复前，按报告中原哈希保留 | [Pi 教学协议](agent-architecture-outcome-pi-20260908.json) |
| DeepSeek 完整产品协议 | 两组均完成并进入学后检查；6.268 / 3.778 秒；与最终安装包源码哈希一致 | [DeepSeek 教学协议](agent-architecture-outcome-deepseek-20260908.json) |
| 六个小样本回答 | Pi 3/3 完成，DeepSeek 2/3 完成；原 R04 被回答检查阻止，未保存足够诊断，不能归因。原报告、未评分状态和失败保留 | [初次回答](agent-architecture-live-20260908.json) |
| DeepSeek R04 独立复测 | 3.108 秒完成，新增支持报告保存三个引用片段、哈希和空问题列表；不覆盖第一次失败，也不证明通用语义正确 | [复测](agent-architecture-grounding-check-20260908.json) |

真实项目验证发现并修复一个模拟契约测试未暴露的 API 兼容问题：`project_checkpoint` 标题正则导出的旧空字符转义 `\0` 被 DeepSeek 返回 HTTP 400。保留原失败和仅记录状态/白名单字段的[诊断](agent-architecture-project-schema-diagnostic-20260908.json)。同一 schema 的[最小真实探针](agent-architecture-schema-probe-20260908.json)仅将其换成 `\u0000`，响应由 400 变 200。加入先失败的导出兼容回归并修复后，两家真实项目流程均通过；DeepSeek 最终七次 HTTP 全为 200。本地仍拒绝空字符、回车和换行，没有移除约束。该具体原因来自探针，不把官方文档的一般 JSON Schema 说明当作此错误的证明。

回答抽查还发现 DeepSeek R01 中“每次资料更新都要重新训练”的表述过强，不能标为完全准确。本次未进行独立人工评分，也未通过调提示使这几题成为表面全绿；有限确定性支持检查不能代替知识准确性复核。以上单次耗时只描述本机和当时服务，不作两家模型长期快慢排名。

完整产品协议真实 smoke 的四段首轮都未主动调用工具；实际工具暴露及隔离由组合契约/UI 测试验证，真实工具执行由独立项目流程验证。脚本学前作答明确标为合成且看过答案，只检查进入学后阶段，没有冒充真实学后提升或三天复习。

#### 0.6.0 本机交付

- `CSC_IDENTITY_AUTO_DISCOVERY=false npm --prefix desktop run dist:mac` 退出 0，生成 macOS arm64 `.app`、DMG 和 ZIP；日志 `/tmp/zhixing-c12-dist-complete.log`。最终源码哈希 `eed07f7f09dee78e17e1e57ad55c06c4698ca29f99b2e79b7ac56917fba26df1`，基线提交 `a1ba9ac`，工作区有本轮未提交修改。代码哈希包含修改内容，不用基线提交冒充交付源码。
- 最终 UI 通过 `ZHIXING_DESKTOP_EXECUTABLE` 指定实际 `desktop/release/mac-arm64/知行.app/Contents/MacOS/知行`，没有把开发 worker 的构建当作实包启动。全部测试使用临时应用数据。
- `hdiutil verify` 退出 0；DMG 只读挂载核对 0.6.0、应用标识、`Applications` 链接及实际 manifest，随后卸载。DMG/ZIP 的字节数和 SHA-256 见[交付清单](delivery-0.6.0-20260908.json)。没有替换 `/Applications` 中的用户安装。
- ZIP 全部条目 CRC 通过；使用 macOS `ditto` 解压至临时目录，中文应用名、版本、manifest 正确，主程序、`app.asar`、Pi worker 字节哈希与通过 UI 的应用一致，随后清理临时解压目录。最初 Python 检查把 ZIP 文件名的默认 CP437 解码当成 UTF-8，导致精确名称断言失败；改用目标平台解压器核对，没有改动安装包或忽略校验错误。
- 根目录与 desktop 的 `npm audit --omit=dev --audit-level=high --registry=https://registry.npmjs.org` 均退出 0，各为零生产依赖漏洞，日志 `/tmp/zhixing-c12-audit-root.log`、`/tmp/zhixing-c12-audit-desktop.log`。
- README、架构/代码导读、配置、CLI、授权、项目、教学、效果验证、测试、开发及故障排查已按 0.6 更新；[0.6 使用与兼容说明](../agent-0.6.md)汇总变化。保留 0.4/0.5 历史记录，不追改旧验收。
- 文档更新后的最终 `npm run verify` 退出 0，仍为 110 文件 / 553 项测试，完整日志 `/tmp/zhixing-c12-verify-delivery.log`；37 份变更 Markdown 中 279 个本地链接全部有效。重新计算源码与实包 manifest，330 个文件及聚合哈希逐项一致。

### 完成范围与下一步

C01–C12 开发及本机验证完成。本轮没有 Git 提交、推送、远端 CI 运行或正式发布；本地验证结果不代表 GitHub Actions 已通过。

以下项目需要实际外部条件，保持待验：真实学习者学前/学后及三天复习、独立人工解释与回答复核、题卷难度校准、指定第三方 MCP、Windows/Intel 实机、Apple Developer ID 签名公证。当前安装包为未签名的本地测试构建。不得据现有合成数据宣称教学效果提升、全面达到商业 Agent 水平或多平台已验证。
