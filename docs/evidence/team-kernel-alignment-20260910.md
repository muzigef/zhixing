# 团队内核架构对齐验证

本记录对应 [TK01–TK05](../agent-team-kernel-plan-20260910.md)。开发前诊断和真实模型分数保留在原报告，本记录不改写它们。

| 切片 | 实际命令与结果 | 边界 |
| --- | --- | --- |
| TK01 | 定向 17/17；`CI=1 npm run verify` exit 0，144 文件 / 729 测试，integration 9、eval 6、mock smoke、敏感扫描与 whitespace 通过 | 可选/必需成员准备失败、主连接失败、30 秒无响应准备、取消、存储失败；未调用真实模型 |
| TK02 | 定向 33/33；完整 verify exit 0，145 文件 / 735 测试，integration 9、eval 6、mock smoke 与扫描通过 | DAG 循环/引用/越权/遗漏校验、依赖阻塞、成员串行、保存先于请求；明确授权后规划/成员/审查共用有界任务包；无授权不转发历史；v11 持久化，旧 v10 保留 |
| TK03 | 定向 31/31；完整 verify exit 0，146 文件 / 738 测试，integration 9、eval 6、mock smoke 与扫描通过 | 显式恢复仅重试指定阶段，不重发成功成员；保留未知计费用量、累计预算、各次执行 ID；本成员公开工作产物用于后续工作；SIGKILL 反重放回归通过；共享服务拒绝已完成/跨任务/超重试上限目标 |
| TK04 | 定向 19/19；完整 verify exit 0，148 文件 / 743 测试，integration 9、eval 6、mock smoke 与扫描通过 | 真实共享只读工具与 SQLite 执行记录；伪造或失败回执不能用来支持命题；逐项覆盖、未解决问题和有限补证后重新审查 |

TK05 的真实 CLI 进程验证和七组隔离 Electron UI 已通过。单次定向补做仅新增3次请求（指定成员、审查、最终综合），没有重发成功同伴。UI 测试首轮补充了展开子任务的实际点击步骤，并修正测试回调参数；产品与任务记录按相同共享契约执行。最终构建后的团队专项 UI 再次通过。

最后补充 sourceTask 更正关系及继续原图片的边界测试。前者证明可以采用新报告覆盖旧条件，保留旧报告；后者验证恢复原图片并拒绝换图。图片测试初版错误要求所有历史与当前图片合计只有一份，已改为逐项核对图片内容，保留既有图片历史策略。后续全量类型检查发现夹具修改 readonly 能力属性，已改用不可变构造；最终全量已通过，见下方结果。

TK02 首轮全量发现一条旧测试仍要求已授权历史不进入审查；按本轮统一任务包契约更新断言，并保留无授权隔离测试，再次全量通过。该变更不是取消隐私约束：主 Agent 系统指令始终不进入任务包，历史作为不可信观察数据。

最终核对修复了评测元数据的旧 quick 标签：新报告为 v4 / 团队协议3，档位来自实际冻结绑定，定向5项评测器测试通过；旧真实报告不更改。尚未使用新内核请求真实 Provider，不能据此宣称团队语义质量提高。

本机运行日志分别为 `/tmp/zhixing-team-kernel-tk01-verify.log`、`/tmp/zhixing-team-kernel-tk02-verify.log`，临时日志不提交。本记录仅证明软件边界与编排行为；真实 Provider 回归、原生钥匙串授权、实际安装和教学成效另验收。

## 最终结果

- `CI=1 npm run verify` exit 0：148 个文件、746 项测试；integration 9、eval 6、mock smoke、lint、根目录/桌面类型检查、敏感扫描及 diff 空白检查通过。
- `ZHIXING_DESKTOP_NATIVE_CIPHER=0 npm --prefix desktop run test:ui` exit 0：聊天、学习、交互、学习效果、项目、API、团队七组通过，网络与加密均为隔离夹具；不能作为本机钥匙串授权结果。
- 最终 `npm --prefix desktop run build` exit 0，团队专项 UI 在最终界面变更后重跑通过。截图已检查，可读且无横向溢出。
- 实际 CLI 进程跨重启补做1个任务，新增3次模型请求，成功同伴仍为1次执行；实际共享工具查询只执行1次，报告伪造回执被拒绝；来源更正关系与原图恢复边界通过。
- 新评测报告 v4 与实际 balanced 成员绑定一致；没有新真实 Provider 分数，不推断语义质量收益。

[机器可读记录与20个源码文件指纹](team-kernel-alignment-20260910.json)固定本轮验收范围。全文档链接核对通过；未提交或推送本轮更改，未替换安装应用。

TK01–TK05 本轮范围完成。仍保留的外部验收是新版真实 Provider 对照、此前系统原生授权问题、签名安装和真实教学成效。任意阶段独立恢复、开放式委派、自由通信与成员写入不在本轮完成声明内，架构边界见[设计文档](../agent-team-kernel.md)。
